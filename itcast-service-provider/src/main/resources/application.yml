server:
  port: 8082
spring:
  datasource:
    url: jdbc:mysql://111.231.104.87:3306/mybatis
    username: root
    password: yixuan
  application:
    name: service-provider # 应用名称，注册到eureka后的服务名称
mybatis:
  type-aliases-package: cn.itcast.service.pojo
eureka:
  client:
    service-url: # EurekaServer地址
      defaultZone: http://127.0.0.1:10086/eureka

